# Nginx config for CMO-FMO SPA at /scan/
# Add this inside your server { } block for hes-consultancy.nl
#
# location /scan/ {
#     alias /var/www/hes-consultancy-nl/scan/;
#     try_files $uri $uri/ /scan/index.html;
# }
